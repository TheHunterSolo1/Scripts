local Library = {}
getgenv().ESPLibrary = Library

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

local Camera = Workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local Cache = {}
local Connection = nil

local Vector2New = Vector2.new
local Vector3New = Vector3.new
local Color3New = Color3.new
local DrawingNew = Drawing and Drawing.new
local Floor = math.floor

local Settings = {
    Tracers = false,
    Distance = false,
    MaxDistance = 5000,
    TextSize = 23,
    Font = Enum.Font.GothamBold,
    TracerThickness = 1,
    FadeTime = 0.5
}

local function GetPosition(obj)
    if not obj:IsDescendantOf(game) then return nil end
    if obj:IsA("BasePart") then
        return obj.Position
    elseif obj:IsA("Model") then
        local rp = obj.PrimaryPart or obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChildWhichIsA("BasePart")
        return rp and rp.Position
    elseif obj:IsA("Attachment") then
        return obj.WorldPosition
    end
    return nil
end

local function GetAdornee(obj)
    if obj:IsA("Model") then
        return obj.PrimaryPart or obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChildWhichIsA("BasePart")
    end
    return obj
end

local function FadeEffect(data, visible, instant)
    local targetTrans = visible and 0 or 1
    local dur = instant and 0 or Settings.FadeTime
    local info = TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    
    if data.Highlight then
        TweenService:Create(data.Highlight, info, {
            FillTransparency = visible and 0.6 or 1,
            OutlineTransparency = targetTrans
        }):Play()
    end
    
    TweenService:Create(data.Label, info, {
        TextTransparency = targetTrans,
        TextStrokeTransparency = visible and 0.2 or 1
    }):Play()

    if data.Tracer then
        TweenService:Create(data.Tracer, info, {
            Transparency = visible and 1 or 0
        }):Play()
    end
end

function Library:SetFont(font)
    Settings.Font = font
    for _, data in pairs(Cache) do
        data.Label.Font = font
    end
end

function Library:SetTracersSize(num)
    Settings.TracerThickness = num
    for _, data in pairs(Cache) do
        if data.Tracer then data.Tracer.Thickness = num end
    end
end

function Library:SetFadeTime(num) Settings.FadeTime = num end
function Library:SetTracers(bool) Settings.Tracers = bool end
function Library:ShowDistance(bool) Settings.Distance = bool end
function Library:SetMaxDistance(num) Settings.MaxDistance = num end

local function CreateVisuals(obj, color, name)
    local adornee = GetAdornee(obj)
    if not adornee or obj:IsDescendantOf(LocalPlayer.Character) then return nil end

    local highlight = nil
    if obj:IsA("Model") or obj:IsA("BasePart") then
        highlight = Instance.new("Highlight")
        highlight.FillColor = color
        highlight.OutlineColor = Color3New(1, 1, 1)
        highlight.FillTransparency = 1
        highlight.OutlineTransparency = 1
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.Adornee = obj
        highlight.Enabled = false
        highlight.Parent = CoreGui
    end

    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0, 250, 0, 60)
    billboard.StudsOffset = Vector3New(0, 2.8, 0)
    billboard.AlwaysOnTop = true
    billboard.Enabled = false
    billboard.Adornee = adornee
    billboard.Parent = CoreGui

    local label = Instance.new("TextLabel")
    label.BackgroundTransparency = 1
    label.Size = UDim2.new(1, 0, 1, 0)
    label.TextColor3 = color
    label.Font = Settings.Font
    label.TextSize = Settings.TextSize
    label.Text = ""
    label.TextTransparency = 1
    label.TextStrokeTransparency = 1
    label.TextStrokeColor3 = Color3New(0, 0, 0)
    label.Parent = billboard

    local tracer = nil
    if DrawingNew then
        tracer = DrawingNew("Line")
        tracer.Visible = false
        tracer.Color = color
        tracer.Thickness = Settings.TracerThickness
        tracer.Transparency = 0
    end

    return {
        Object = obj,
        Highlight = highlight,
        Gui = billboard,
        Label = label,
        Tracer = tracer,
        Name = name,
        Active = false,
        LastText = "",
        Adornee = adornee
    }
end

function Library:AddESP(obj, name, color)
    if not obj or Cache[obj] then return end
    local data = CreateVisuals(obj, color, name)
    if data then 
        Cache[obj] = data 
        obj.AncestryChanged:Connect(function(_, parent)
            if not parent then Library:RemoveESP(obj) end
        end)
        FadeEffect(data, true, false)
    end
end

function Library:RemoveESP(obj)
    local data = Cache[obj]
    if data then
        data.Active = false
        FadeEffect(data, false, false)
        Cache[obj] = nil
        task.delay(Settings.FadeTime, function()
            if data.Highlight then data.Highlight:Destroy() end
            data.Gui:Destroy()
            if data.Tracer then data.Tracer:Remove() end
        end)
    end
end

local function Update()
    Camera = Workspace.CurrentCamera
    local CamCF = Camera.CFrame
    local CamPos = CamCF.Position
    local CamFOV = Camera.FieldOfView
    local ViewportSize = Camera.ViewportSize
    local Center = Vector2New(ViewportSize.X / 2, ViewportSize.Y)
    
    local fovScale = CamFOV / 70

    for obj, data in pairs(Cache) do
        local pos = GetPosition(obj)
        if not pos then continue end

        local dist = (CamPos - pos).Magnitude
        local screenPos, onScreen = Camera:WorldToViewportPoint(pos)

        if onScreen and dist <= Settings.MaxDistance then
            if not data.Active then
                data.Active = true
                data.Gui.Enabled = true
                if data.Highlight then data.Highlight.Enabled = true end
                if data.Tracer then data.Tracer.Visible = true end
            end

            data.Label.TextSize = Settings.TextSize * fovScale

            local currentText = Settings.Distance and (data.Name .. " [" .. Floor(dist) .. "m]") or data.Name
            if data.LastText ~= currentText then
                data.Label.Text = currentText
                data.LastText = currentText
            end

            if data.Tracer then
                if Settings.Tracers then
                    data.Tracer.From = Center
                    data.Tracer.To = Vector2New(screenPos.X, screenPos.Y)
                else
                    data.Tracer.Transparency = 0
                end
            end
        else
            if data.Active then
                data.Active = false
                data.Gui.Enabled = false
                if data.Highlight then data.Highlight.Enabled = false end
                if data.Tracer then data.Tracer.Visible = false end
            end
        end
    end
end

Connection = RunService.RenderStepped:Connect(Update)

function Library:Unload()
    if Connection then Connection:Disconnect() end
    for obj, data in pairs(Cache) do 
        if data.Highlight then data.Highlight:Destroy() end
        data.Gui:Destroy()
        if data.Tracer then data.Tracer:Remove() end
    end
    table.clear(Cache)
    getgenv().ESPLibrary = nil
end

return Library
