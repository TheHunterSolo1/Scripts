local UserInputService = game:GetService("UserInputService")
local RS = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")

local Device = UserInputService.TouchEnabled and "Mobile" or UserInputService.KeyboardEnabled and "PC"
local CoreGui = gethui and gethui() or game:GetService("CoreGui")

local Library = {
    Settings = {
        Tracers = true,
        Distance = true,
        Arrows = true,
        Rainbow = false,
        MaxDistance = 999999,
        TextSize = 25,
        Font = Enum.Font.RobotoMono,
        FadeTime = 0.7,
        ESPMode = "Highlight/Text",
        RenderTime = (Device == "PC" and 240 or Device == "Mobile" and 30) or 60,

        LOD100 = 1,
        LOD250 = 0.75,
        LOD500 = 0.5,
        LOD1000 = 0.25,
        LODFar = 0.1,

        AdaptiveLOD = true,
        MovementPriority = true,
        FrameBudget = 100,
        MetadataInterval = 0.12,
        RainbowRate = 30,
        CleanupInterval = 2,
    },

    Cache = {},
    TweenInfoFadeIn = nil,
    TweenInfoFadeOut = nil,

    ESPCount = 0,

    Scheduler = {},
    SchedulerSize = 0,
    SchedulerEpoch = 0,

    LastCleanup = 0,
    LastRainbow = 0,
    RainbowColor = Color3.fromHSV(0, 1, 1),

    LastCamera = nil,
    LastViewportX = 0,
    LastViewportY = 0,
    LastFOV = 0,

    Connection = nil,
}

local Screen = Instance.new("ScreenGui")
Screen.Name = HttpService:GenerateGUID(false)
Screen.IgnoreGuiInset = true
Screen.Parent = CoreGui

local Camera = workspace.CurrentCamera

local function GetRoot(Obj)
    if not Obj then
        return nil
    end

    if Obj:IsA("Model") then
        return Obj:FindFirstChild("HumanoidRootPart")
            or Obj.PrimaryPart
            or Obj:FindFirstChildWhichIsA("BasePart")
    end

    if Obj:IsA("BasePart") then
        return Obj
    end

    return nil
end

local function GetExistingHighlight(Obj)
    if not Obj then
        return nil
    end

    return Obj:FindFirstChildWhichIsA("Highlight", true)
end

local function HeapPush(Heap, Size, Node)
    Size += 1

    local Index = Size

    while Index > 1 do
        local Parent = math.floor(Index / 2)

        if Heap[Parent].Time <= Node.Time then
            break
        end

        Heap[Index] = Heap[Parent]
        Index = Parent
    end

    Heap[Index] = Node

    return Size
end

local function HeapPop(Heap, Size)
    if Size == 0 then
        return nil, 0
    end

    local Root = Heap[1]
    local Last = Heap[Size]

    Heap[Size] = nil
    Size -= 1

    if Size == 0 then
        return Root, 0
    end

    local Index = 1

    while true do
        local Left = Index * 2

        if Left > Size then
            break
        end

        local Right = Left + 1
        local Child = Left

        if Right <= Size and Heap[Right].Time < Heap[Left].Time then
            Child = Right
        end

        if Heap[Child].Time >= Last.Time then
            break
        end

        Heap[Index] = Heap[Child]
        Index = Child
    end

    Heap[Index] = Last

    return Root, Size
end

local function GetLODInterval(DistanceSquared, RenderTime, Settings, ESPCount)
    local SpeedMultiplier

    if DistanceSquared <= 10000 then
        SpeedMultiplier = Settings.LOD100
    elseif DistanceSquared <= 62500 then
        SpeedMultiplier = Settings.LOD250
    elseif DistanceSquared <= 250000 then
        SpeedMultiplier = Settings.LOD500
    elseif DistanceSquared <= 1000000 then
        SpeedMultiplier = Settings.LOD1000
    else
        SpeedMultiplier = Settings.LODFar
    end

    SpeedMultiplier = math.max(SpeedMultiplier, 0.01)

    if Settings.AdaptiveLOD then
        if ESPCount > 150 then
            SpeedMultiplier *= 0.45
        elseif ESPCount > 100 then
            SpeedMultiplier *= 0.6
        elseif ESPCount > 60 then
            SpeedMultiplier *= 0.8
        end
    end

    return math.max(
        1 / math.max(RenderTime, 1) / SpeedMultiplier,
        1 / 60
    )
end

local function SetVisible(Data, Tag, Text, Box, Line, Arrow)
    if Data.LastTagEnabled ~= Tag then
        Data.Tag.Enabled = Tag
        Data.LastTagEnabled = Tag
    end

    if Data.LastTextVisible ~= Text then
        Data.Text.Visible = Text
        Data.LastTextVisible = Text
    end

    if Data.LastBoxEnabled ~= Box then
        Data.Box.Enabled = Box
        Data.LastBoxEnabled = Box
    end

    if Data.LastLineVisible ~= Line then
        Data.Line.Visible = Line
        Data.LastLineVisible = Line
    end

    if Data.LastArrowVisible ~= Arrow then
        Data.Arrow.Visible = Arrow
        Data.LastArrowVisible = Arrow
    end
end

local function SetTextColor(Data, Color)
    if Data.LastTextColor ~= Color then
        Data.Text.TextColor3 = Color
        Data.LastTextColor = Color
    end
end

local function SetHighlightColor(Data, Color)
    if Data.LastHighlightColor ~= Color then
        Data.Box.FillColor = Color
        Data.LastHighlightColor = Color
    end
end

local function SetLineColor(Data, Color)
    if Data.LastLineColor ~= Color then
        Data.Line.BackgroundColor3 = Color
        Data.LastLineColor = Color
    end
end

local function SetArrowColor(Data, Color)
    if Data.LastArrowColor ~= Color then
        Data.Arrow.ImageColor3 = Color
        Data.LastArrowColor = Color
    end
end

local function ScheduleData(Data, Time)
    Library.SchedulerSize = HeapPush(
        Library.Scheduler,
        Library.SchedulerSize,
        {
            Time = Time,
            Object = Data.Object,
            Version = Data.ScheduleVersion,
            Epoch = Library.SchedulerEpoch,
        }
    )
end

local function ScheduleObject(Data, Time)
    Data.NextUpdate = Time
    Data.ScheduleVersion += 1
    ScheduleData(Data, Time)
end

local function InvalidateScheduler()
    Library.SchedulerEpoch += 1
    Library.Scheduler = {}
    Library.SchedulerSize = 0

    local Now = os.clock()

    for _, Data in pairs(Library.Cache) do
        Data.ScheduleVersion += 1
        Data.NextUpdate = Now

        ScheduleData(Data, Now)
    end
end

function Library:RebuildTweenInfo()
    local FT = math.max(self.Settings.FadeTime, 0)

    self.TweenInfoFadeIn = TweenInfo.new(
        FT,
        Enum.EasingStyle.Linear,
        Enum.EasingDirection.Out
    )

    self.TweenInfoFadeOut = TweenInfo.new(
        FT,
        Enum.EasingStyle.Linear,
        Enum.EasingDirection.Out
    )
end

Library:RebuildTweenInfo()

function Library:SetFont(Font)
    self.Settings.Font = Font

    for _, Data in pairs(self.Cache) do
        if Data.Text.Font ~= Font then
            Data.Text.Font = Font
        end
    end
end

function Library:SetRainbow(Bool)
    self.Settings.Rainbow = Bool
    InvalidateScheduler()
end

function Library:SetFadeTime(Value)
    self.Settings.FadeTime = math.max(Value, 0)
    self:RebuildTweenInfo()
end

function Library:SetArrows(Bool)
    self.Settings.Arrows = Bool
    InvalidateScheduler()
end

function Library:SetTracers(Bool)
    self.Settings.Tracers = Bool
    InvalidateScheduler()
end

function Library:SetShowDistance(Bool)
    self.Settings.Distance = Bool
    InvalidateScheduler()
end

function Library:SetESPMode(Value)
    self.Settings.ESPMode = Value
    InvalidateScheduler()
end

function Library:SetRenderingSpeed(Value)
    self.Settings.RenderTime = math.max(Value, 1)
    InvalidateScheduler()
end

function Library:HasESP(Obj)
    return self.Cache[Obj] ~= nil
end

function Library:GetESPData(Obj)
    return self.Cache[Obj]
end

function Library:GetAllESP()
    local Result = {}

    for Obj, Data in pairs(self.Cache) do
        Result[Obj] = {
            Name = Data.Name,
            Color = Data.Color,
            Root = Data.Root,
        }
    end

    return Result
end

function Library:CountESP()
    return self.ESPCount
end

function Library:AddESP(Cfg)
    local Obj = Cfg.Object

    if not Obj or self.Cache[Obj] then
        return
    end

    local Root = GetRoot(Obj)

    if not Root then
        task.spawn(function()
            local Attempts = 0

            while Obj.Parent and Attempts < 60 do
                Root = GetRoot(Obj)

                if Root then
                    break
                end

                Attempts += 1
                task.wait(0.5)
            end

            if Root and Obj.Parent and not self.Cache[Obj] then
                self:AddESP(Cfg)
            end
        end)

        return
    end

    local InitialColor = Cfg.Color
    local InitialText = Cfg.Text

    local Billboard = Instance.new("BillboardGui")
    Billboard.Name = HttpService:GenerateGUID(false)
    Billboard.Size = UDim2.new(0, 250, 0, 60)
    Billboard.AlwaysOnTop = true
    Billboard.Adornee = Root
    Billboard.MaxDistance = self.Settings.MaxDistance
    Billboard.Parent = Obj

    local Label = Instance.new("TextLabel")
    Label.BackgroundTransparency = 1
    Label.Size = UDim2.new(1, 0, 1, 0)
    Label.TextColor3 = InitialColor
    Label.Font = self.Settings.Font
    Label.TextSize = self.Settings.TextSize
    Label.TextStrokeTransparency = 0
    Label.TextTransparency = 1
    Label.Text = InitialText
    Label.Parent = Billboard

    local Highlight = GetExistingHighlight(Obj)
    local OwnsHighlight = false

    if not Highlight then
        Highlight = Instance.new("Highlight")
        Highlight.Adornee = Obj
        Highlight.Parent = Obj
        OwnsHighlight = true
    end

    local OriginalHighlight = {
        Enabled = Highlight.Enabled,
        Adornee = Highlight.Adornee,
        FillColor = Highlight.FillColor,
        FillTransparency = Highlight.FillTransparency,
        OutlineColor = Highlight.OutlineColor,
        OutlineTransparency = Highlight.OutlineTransparency,
        DepthMode = Highlight.DepthMode,
    }

    Highlight.Adornee = Obj
    Highlight.FillColor = InitialColor
    Highlight.FillTransparency = 1
    Highlight.OutlineTransparency = 1
    Highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop

    local Line = Instance.new("Frame")
    Line.AnchorPoint = Vector2.new(0.5, 0.5)
    Line.BorderSizePixel = 0
    Line.BackgroundColor3 = InitialColor
    Line.Visible = true
    Line.BackgroundTransparency = 1
    Line.Parent = Screen

    local Arrow = Instance.new("ImageLabel")
    Arrow.BackgroundTransparency = 1
    Arrow.AnchorPoint = Vector2.new(0.5, 0.5)
    Arrow.Size = UDim2.new(0, 32, 0, 32)
    Arrow.Image = "rbxassetid://10526559647"
    Arrow.ImageColor3 = InitialColor
    Arrow.Visible = false
    Arrow.Parent = Screen

    local FT = self.Settings.FadeTime

    if FT > 0 then
        TweenService:Create(
            Label,
            self.TweenInfoFadeIn,
            {TextTransparency = 0}
        ):Play()

        TweenService:Create(
            Highlight,
            self.TweenInfoFadeIn,
            {FillTransparency = 0.6}
        ):Play()

        TweenService:Create(
            Line,
            self.TweenInfoFadeIn,
            {BackgroundTransparency = 0}
        ):Play()
    else
        Label.TextTransparency = 0
        Highlight.FillTransparency = 0.6
        Line.BackgroundTransparency = 0
    end

    local Data = {
        Object = Obj,

        Tag = Billboard,
        Text = Label,
        Line = Line,
        Box = Highlight,
        Arrow = Arrow,

        Name = InitialText,
        Color = InitialColor,
        Root = Root,

        OwnsHighlight = OwnsHighlight,
        OriginalHighlight = OriginalHighlight,

        NextUpdate = 0,
        NextMetadataUpdate = 0,

        LastPosition = Root.Position,
        LastDistance = -1,
        LastDisplayedDistance = -1,

        LastTextColor = InitialColor,
        LastHighlightColor = InitialColor,
        LastLineColor = InitialColor,
        LastArrowColor = InitialColor,

        LastTextSize = self.Settings.TextSize,

        LastTagEnabled = true,
        LastTextVisible = false,
        LastBoxEnabled = false,
        LastLineVisible = true,
        LastArrowVisible = false,

        LastTracerX = -1,
        LastTracerY = -1,
        LastTracerLength = -1,
        LastTracerRotation = -999,

        LastArrowX = -1,
        LastArrowY = -1,
        LastArrowRotation = -999,

        LODLevel = -1,
        ScheduleVersion = 0,
        FadeToken = 0,
    }

    self.Cache[Obj] = Data
    self.ESPCount += 1

    local Now = os.clock()

    Data.NextMetadataUpdate = Now
    ScheduleObject(Data, Now)
end

function Library:RemoveESP(Obj)
    local Data = self.Cache[Obj]

    if not Data then
        return
    end

    self.Cache[Obj] = nil
    self.ESPCount = math.max(self.ESPCount - 1, 0)

    Data.ScheduleVersion += 1

    local FT = self.Settings.FadeTime

    if FT > 0 then
        Data.FadeToken += 1

        local Token = Data.FadeToken

        TweenService:Create(
            Data.Text,
            self.TweenInfoFadeOut,
            {TextTransparency = 1}
        ):Play()

        TweenService:Create(
            Data.Box,
            self.TweenInfoFadeOut,
            {FillTransparency = 1}
        ):Play()

        TweenService:Create(
            Data.Line,
            self.TweenInfoFadeOut,
            {BackgroundTransparency = 1}
        ):Play()

        task.delay(FT, function()
            if Data.FadeToken ~= Token then
                return
            end

            if Data.OwnsHighlight then
                if Data.Box then
                    Data.Box:Destroy()
                end
            elseif Data.Box and Data.Box.Parent then
                local Original = Data.OriginalHighlight

                Data.Box.Enabled = Original.Enabled
                Data.Box.Adornee = Original.Adornee
                Data.Box.FillColor = Original.FillColor
                Data.Box.FillTransparency = Original.FillTransparency
                Data.Box.OutlineColor = Original.OutlineColor
                Data.Box.OutlineTransparency = Original.OutlineTransparency
                Data.Box.DepthMode = Original.DepthMode
            end

            if Data.Tag then
                Data.Tag:Destroy()
            end

            if Data.Line then
                Data.Line:Destroy()
            end

            if Data.Arrow then
                Data.Arrow:Destroy()
            end
        end)
    else
        if Data.OwnsHighlight then
            if Data.Box then
                Data.Box:Destroy()
            end
        elseif Data.Box and Data.Box.Parent then
            local Original = Data.OriginalHighlight

            Data.Box.Enabled = Original.Enabled
            Data.Box.Adornee = Original.Adornee
            Data.Box.FillColor = Original.FillColor
            Data.Box.FillTransparency = Original.FillTransparency
            Data.Box.OutlineColor = Original.OutlineColor
            Data.Box.OutlineTransparency = Original.OutlineTransparency
            Data.Box.DepthMode = Original.DepthMode
        end

        if Data.Tag then
            Data.Tag:Destroy()
        end

        if Data.Line then
            Data.Line:Destroy()
        end

        if Data.Arrow then
            Data.Arrow:Destroy()
        end
    end
end

function Library:UpdateESP(Obj, NewText, NewColor)
    local Data = self.Cache[Obj]

    if Data then
        if NewText and Data.Name ~= NewText then
            Data.Name = NewText
            Data.Text.Text = NewText
            Data.LastDisplayedDistance = -1
            Data.NextMetadataUpdate = 0
        end

        if NewColor and Data.Color ~= NewColor then
            Data.Color = NewColor

            Data.Text.TextColor3 = NewColor
            Data.Box.FillColor = NewColor
            Data.Line.BackgroundColor3 = NewColor
            Data.Arrow.ImageColor3 = NewColor

            Data.LastTextColor = NewColor
            Data.LastHighlightColor = NewColor
            Data.LastLineColor = NewColor
            Data.LastArrowColor = NewColor
        end

        ScheduleObject(Data, os.clock())
    else
        self:AddESP({
            Object = Obj,
            Text = NewText or "ESP",
            Color = NewColor or Color3.new(1, 1, 1),
        })
    end
end

function Library:BulkUpdateESP(UpdateTable)
    local Now = os.clock()

    for Obj, Info in pairs(UpdateTable) do
        local Data = self.Cache[Obj]

        if Data then
            if Info.Text and Data.Name ~= Info.Text then
                Data.Name = Info.Text
                Data.Text.Text = Info.Text
                Data.LastDisplayedDistance = -1
                Data.NextMetadataUpdate = 0
            end

            if Info.Color and Data.Color ~= Info.Color then
                local Color = Info.Color

                Data.Color = Color

                Data.Text.TextColor3 = Color
                Data.Box.FillColor = Color
                Data.Line.BackgroundColor3 = Color
                Data.Arrow.ImageColor3 = Color

                Data.LastTextColor = Color
                Data.LastHighlightColor = Color
                Data.LastLineColor = Color
                Data.LastArrowColor = Color
            end

            ScheduleObject(Data, Now)
        elseif Info.Text and Info.Color then
            self:AddESP({
                Object = Obj,
                Text = Info.Text,
                Color = Info.Color,
            })
        end
    end
end

function Library:ClearAllESP()
    local Objects = {}

    for Obj in pairs(self.Cache) do
        Objects[#Objects + 1] = Obj
    end

    for i = 1, #Objects do
        self:RemoveESP(Objects[i])
    end
end

function Library:Unload()
    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end

    self.Scheduler = {}
    self.SchedulerSize = 0
    self.SchedulerEpoch += 1

    for Obj, Data in pairs(self.Cache) do
        self.Cache[Obj] = nil

        if Data.OwnsHighlight then
            if Data.Box then
                Data.Box:Destroy()
            end
        elseif Data.Box and Data.Box.Parent then
            local Original = Data.OriginalHighlight

            Data.Box.Enabled = Original.Enabled
            Data.Box.Adornee = Original.Adornee
            Data.Box.FillColor = Original.FillColor
            Data.Box.FillTransparency = Original.FillTransparency
            Data.Box.OutlineColor = Original.OutlineColor
            Data.Box.OutlineTransparency = Original.OutlineTransparency
            Data.Box.DepthMode = Original.DepthMode
        end

        if Data.Tag then
            Data.Tag:Destroy()
        end

        if Data.Line then
            Data.Line:Destroy()
        end

        if Data.Arrow then
            Data.Arrow:Destroy()
        end
    end

    self.ESPCount = 0

    if Screen then
        Screen:Destroy()
    end
end

Camera = workspace.CurrentCamera

local ScreenCenter = Camera.ViewportSize / 2
local ViewportSize = Camera.ViewportSize
local FOVScale = Camera.FieldOfView / 70

Library.LastCamera = Camera
Library.LastViewportX = ViewportSize.X
Library.LastViewportY = ViewportSize.Y
Library.LastFOV = Camera.FieldOfView

local LastHeartbeat = os.clock()

Library.Connection = RS.Heartbeat:Connect(function()
    local Now = os.clock()
    local Settings = Library.Settings

    Camera = workspace.CurrentCamera

    if not Camera then
        return
    end

    local CurrentViewport = Camera.ViewportSize
    local CurrentFOV = Camera.FieldOfView

    local CameraChanged = Camera ~= Library.LastCamera

    local ViewportChanged =
        CurrentViewport.X ~= Library.LastViewportX
        or CurrentViewport.Y ~= Library.LastViewportY

    local FOVChanged = CurrentFOV ~= Library.LastFOV

    if CameraChanged or ViewportChanged or FOVChanged then
        Library.LastCamera = Camera
        Library.LastViewportX = CurrentViewport.X
        Library.LastViewportY = CurrentViewport.Y
        Library.LastFOV = CurrentFOV

        ViewportSize = CurrentViewport
        ScreenCenter = CurrentViewport / 2
        FOVScale = CurrentFOV / 70

        InvalidateScheduler()
    end

    if Now - Library.LastRainbow >= 1 / math.max(Settings.RainbowRate, 1) then
        Library.LastRainbow = Now
        Library.RainbowColor = Color3.fromHSV((Now % 5) / 5, 1, 1)
    end

    if Now - Library.LastCleanup >= Settings.CleanupInterval then
        Library.LastCleanup = Now

        for Object, Data in pairs(Library.Cache) do
            if not Object.Parent then
                Library:RemoveESP(Object)
            elseif not Data.Root or not Data.Root.Parent then
                Data.Root = GetRoot(Object)
            end

            if not Data.Box or not Data.Box.Parent then
                if Data.OwnsHighlight then
                    Data.Box = Instance.new("Highlight")
                    Data.Box.Adornee = Object
                    Data.Box.FillColor = Data.Color
                    Data.Box.FillTransparency = 1
                    Data.Box.OutlineTransparency = 1
                    Data.Box.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    Data.Box.Parent = Object

                    Data.LastHighlightColor = Data.Color
                else
                    Data.Box = GetExistingHighlight(Object)
                end
            end
        end
    end

    local RenderInterval =
        1 / math.max(Settings.RenderTime, 1)

    if Now - LastHeartbeat < RenderInterval then
        return
    end

    LastHeartbeat = Now

    local Processed = 0
    local Budget = math.max(Settings.FrameBudget, 1)

    while Library.SchedulerSize > 0 and Processed < Budget do
        local Node

        Node, Library.SchedulerSize =
            HeapPop(
                Library.Scheduler,
                Library.SchedulerSize
            )

        if not Node then
            break
        end

        if Node.Epoch ~= Library.SchedulerEpoch then
            continue
        end

        local Object = Node.Object
        local Data = Library.Cache[Object]

        if not Data then
            continue
        end

        if Data.ScheduleVersion ~= Node.Version then
            continue
        end

        if Node.Time > Now then
            Library.SchedulerSize =
                HeapPush(
                    Library.Scheduler,
                    Library.SchedulerSize,
                    Node
                )

            break
        end

        Processed += 1

        if not Object.Parent then
            Library:RemoveESP(Object)
            continue
        end

        local RootPart = Data.Root

        if not RootPart or not RootPart.Parent then
            RootPart = GetRoot(Object)
            Data.Root = RootPart

            if not RootPart then
                ScheduleObject(Data, Now + 0.5)
                continue
            end
        end

        local Position = RootPart.Position
        local CameraCFrame = Camera.CFrame
        local CameraPosition = CameraCFrame.Position

        local DX = Position.X - CameraPosition.X
        local DY = Position.Y - CameraPosition.Y
        local DZ = Position.Z - CameraPosition.Z

        local DistanceSquared =
            DX * DX
            + DY * DY
            + DZ * DZ

        local MaxDistance = Settings.MaxDistance
        local MaxDistanceSquared = MaxDistance * MaxDistance

        if DistanceSquared > MaxDistanceSquared then
            SetVisible(
                Data,
                false,
                false,
                false,
                false,
                false
            )

            ScheduleObject(
                Data,
                Now + GetLODInterval(
                    DistanceSquared,
                    Settings.RenderTime,
                    Settings,
                    Library.ESPCount
                )
            )

            continue
        end

        local Distance = math.sqrt(DistanceSquared)

        local LastPosition = Data.LastPosition

        local MovementDX = Position.X - LastPosition.X
        local MovementDY = Position.Y - LastPosition.Y
        local MovementDZ = Position.Z - LastPosition.Z

        local MovementSquared =
            MovementDX * MovementDX
            + MovementDY * MovementDY
            + MovementDZ * MovementDZ

        Data.LastPosition = Position

        local Moving = MovementSquared > 0.0004

        if DistanceSquared <= 10000 then
            Data.LODLevel = 1
        elseif DistanceSquared <= 62500 then
            Data.LODLevel = 2
        elseif DistanceSquared <= 250000 then
            Data.LODLevel = 3
        elseif DistanceSquared <= 1000000 then
            Data.LODLevel = 4
        else
            Data.LODLevel = 5
        end

        local ScreenPosition, IsOnScreen =
            Camera:WorldToViewportPoint(Position)

        local DisplayColor =
            Settings.Rainbow
            and Library.RainbowColor
            or Data.Color

        if IsOnScreen then
            local ESPMode = Settings.ESPMode

            local ShowHighlight =
                ESPMode == "Highlight"
                or ESPMode == "Highlight/Text"

            local ShowText =
                ESPMode == "Text"
                or ESPMode == "Highlight/Text"

            SetVisible(
                Data,
                true,
                ShowText,
                ShowHighlight,
                Settings.Tracers and Distance < 1500,
                false
            )

            if ShowText then
                SetTextColor(Data, DisplayColor)

                local TextSize =
                    Settings.TextSize * FOVScale

                if Data.LastTextSize ~= TextSize then
                    Data.Text.TextSize = TextSize
                    Data.LastTextSize = TextSize
                end

                if Now >= Data.NextMetadataUpdate then
                    local DistanceFloor = math.floor(Distance)

                    if Settings.Distance then
                        if Data.LastDisplayedDistance ~= DistanceFloor then
                            Data.Text.Text =
                                Data.Name
                                .. "\n["
                                .. DistanceFloor
                                .. "m]"

                            Data.LastDisplayedDistance =
                                DistanceFloor
                        end
                    elseif Data.LastDisplayedDistance ~= -2 then
                        Data.Text.Text = Data.Name
                        Data.LastDisplayedDistance = -2
                    end

                    Data.NextMetadataUpdate =
                        Now + Settings.MetadataInterval
                end
            end

            if ShowHighlight and Data.Box and Data.Box.Parent then
                SetHighlightColor(Data, DisplayColor)
            end

            if Settings.Tracers and Distance < 1500 then
                SetLineColor(Data, DisplayColor)

                local SX = ScreenPosition.X
                local SY = ScreenPosition.Y

                local OriginX = ScreenCenter.X
                local OriginY = ViewportSize.Y

                local DifferenceX = SX - OriginX
                local DifferenceY = SY - OriginY

                local Length = math.sqrt(
                    DifferenceX * DifferenceX
                    + DifferenceY * DifferenceY
                )

                local Rotation = math.deg(
                    math.atan2(
                        DifferenceY,
                        DifferenceX
                    )
                )

                if
                    Data.LastTracerX ~= SX
                    or Data.LastTracerY ~= SY
                    or Data.LastTracerLength ~= Length
                    or Data.LastTracerRotation ~= Rotation
                then
                    Data.Line.Size =
                        UDim2.new(0, Length, 0, 1)

                    Data.Line.Position =
                        UDim2.new(
                            0,
                            OriginX + DifferenceX * 0.5,
                            0,
                            OriginY + DifferenceY * 0.5
                        )

                    Data.Line.Rotation = Rotation

                    Data.LastTracerX = SX
                    Data.LastTracerY = SY
                    Data.LastTracerLength = Length
                    Data.LastTracerRotation = Rotation
                end
            end
        else
            SetVisible(
                Data,
                false,
                false,
                false,
                false,
                Settings.Arrows
            )

            if Settings.Arrows then
                SetArrowColor(Data, DisplayColor)

                local Relative =
                    CameraCFrame:PointToObjectSpace(Position)

                local DirectionAngle =
                    math.atan2(
                        -Relative.X,
                        -Relative.Z
                    )

                local Radius =
                    ViewportSize.Y * 0.4

                local ArrowX =
                    ScreenCenter.X
                    + math.sin(DirectionAngle) * Radius

                local ArrowY =
                    ScreenCenter.Y
                    + math.cos(DirectionAngle) * Radius

                local ArrowRotation =
                    math.deg(DirectionAngle)

                if
                    Data.LastArrowX ~= ArrowX
                    or Data.LastArrowY ~= ArrowY
                    or Data.LastArrowRotation ~= ArrowRotation
                then
                    Data.Arrow.Position =
                        UDim2.new(
                            0,
                            ArrowX,
                            0,
                            ArrowY
                        )

                    Data.Arrow.Rotation =
                        ArrowRotation

                    Data.LastArrowX = ArrowX
                    Data.LastArrowY = ArrowY
                    Data.LastArrowRotation =
                        ArrowRotation
                end
            end
        end

        local Interval =
            GetLODInterval(
                DistanceSquared,
                Settings.RenderTime,
                Settings,
                Library.ESPCount
            )

        if Settings.MovementPriority then
            if Moving then
                Interval *= 0.55
            else
                Interval *= 1.15
            end
        end

        ScheduleObject(
            Data,
            Now + math.max(
                Interval,
                1 / 60
            )
        )
    end
end)

return Library